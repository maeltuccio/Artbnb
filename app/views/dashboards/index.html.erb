<h1>Dashboard</h1>

<h2>Mes réservations en cours</h2>

<% if @current_bookings.any? %>
  <table class="table">
    <thead>
      <tr>
        <th>Artwork</th>
        <th>Date de début</th>
        <th>Date de fin</th>
        <th>Status</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
      <% @current_bookings.each do |booking| %>
        <tr>
          <td><%= booking.artwork.title %></td>
          <td><%= booking.start_date.strftime('%d/%m/%Y') %></td>
          <td><%= booking.end_date.strftime('%d/%m/%Y') %></td>
          <td><%= booking.status %></td>
          <td>
            <% if booking.status == 'Pending' %>
              <%= button_to 'Accepter', accept_dashboard_path(booking), method: :patch, class: 'btn btn-success' %>
              <%= button_to 'Refuser', decline_dashboard_path(booking), method: :patch, class: 'btn btn-danger' %>
            <% else %>
              <span class="badge badge-secondary">Statut: <%= booking.status %></span>
            <% end %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <p>Vous n'avez aucune réservation en cours.</p>
<% end %>
